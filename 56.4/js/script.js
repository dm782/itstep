document.addEventListener('keydown', function (event) { // Слушатель событий при нажатии
    var div = document.getElementById("myDivZero"); // Ищу элемент по id из HTML
    if (event.code == "Digit0") { // Если нажата "1"
        div.style.backgroundColor = "green"; // Изменить цвет на зелёный
    }
});

document.addEventListener('keyup', function (event) { // Слушатель событий при нажатии
    var div = document.getElementById("myDivZero"); // Ищу элемент по id из HTML
    if (event.code == "Digit0") { // Если нажата "1"
        div.style.backgroundColor = "brown"; // Изменить цвет на зелёный
    }
});

document.addEventListener('keydown', function (event) { // Слушатель событий при нажатии
    var div = document.getElementById("myDivOne"); // Ищу элемент по id из HTML
    if(event.code == "Digit1"){ // Если нажата "1"
        div.style.backgroundColor = "green"; // Изменить цвет на зелёный
    }
});

document.addEventListener('keyup', function (event) { // Слушатель событий при нажатии
    var div = document.getElementById("myDivOne"); // Ищу элемент по id из HTML
    if (event.code == "Digit1") { // Если нажата "1"
        div.style.backgroundColor = "brown"; // Изменить цвет на зелёный
    }
});

document.addEventListener('keydown', function (event) { // Слушатель событий при нажатии
    var div = document.getElementById("myDivTwo"); // Ищу элемент по id из HTML
    if (event.code == "Digit2") { // Если нажата "1"
        div.style.backgroundColor = "green"; // Изменить цвет на зелёный
    }
});
document.addEventListener('keyup', function (event) { // Слушатель событий при нажатии
    var div = document.getElementById("myDivTwo"); // Ищу элемент по id из HTML
    if (event.code == "Digit2") { // Если нажата "1"
        div.style.backgroundColor = "brown"; // Изменить цвет на зелёный
    }
});

document.addEventListener('keydown', function (event) { // Слушатель событий при нажатии
    var div = document.getElementById("myDivThree"); // Ищу элемент по id из HTML
    if (event.code == "Digit3") { // Если нажата "1"
        div.style.backgroundColor = "green"; // Изменить цвет на зелёный
    }
});
document.addEventListener('keyup', function (event) { // Слушатель событий при нажатии
    var div = document.getElementById("myDivThree"); // Ищу элемент по id из HTML
    if (event.code == "Digit3") { // Если нажата "1"
        div.style.backgroundColor = "brown"; // Изменить цвет на зелёный
    }
});

document.addEventListener('keydown', function (event) { // Слушатель событий при нажатии
    var div = document.getElementById("myDivFour"); // Ищу элемент по id из HTML
    if (event.code == "Digit4") { // Если нажата "1"
        div.style.backgroundColor = "green"; // Изменить цвет на зелёный
    }
});
document.addEventListener('keyup', function (event) { // Слушатель событий при нажатии
    var div = document.getElementById("myDivFour"); // Ищу элемент по id из HTML
    if (event.code == "Digit4") { // Если нажата "1"
        div.style.backgroundColor = "brown"; // Изменить цвет на зелёный
    }
});

document.addEventListener('keydown', function (event) { // Слушатель событий при нажатии
    var div = document.getElementById("myDivFive"); // Ищу элемент по id из HTML
    if (event.code == "Digit5") { // Если нажата "1"
        div.style.backgroundColor = "green"; // Изменить цвет на зелёный
    }
});
document.addEventListener('keyup', function (event) { // Слушатель событий при нажатии
    var div = document.getElementById("myDivFive"); // Ищу элемент по id из HTML
    if (event.code == "Digit5") { // Если нажата "1"
        div.style.backgroundColor = "brown"; // Изменить цвет на зелёный
    }
});

document.addEventListener('keydown', function (event) { // Слушатель событий при нажатии
    var div = document.getElementById("myDivSix"); // Ищу элемент по id из HTML
    if (event.code == "Digit6") { // Если нажата "1"
        div.style.backgroundColor = "green"; // Изменить цвет на зелёный
    }
});
document.addEventListener('keyup', function (event) { // Слушатель событий при нажатии
    var div = document.getElementById("myDivSix"); // Ищу элемент по id из HTML
    if (event.code == "Digit6") { // Если нажата "1"
        div.style.backgroundColor = "brown"; // Изменить цвет на зелёный
    }
});

document.addEventListener('keydown', function (event) { // Слушатель событий при нажатии
    var div = document.getElementById("myDivSeven"); // Ищу элемент по id из HTML
    if (event.code == "Digit7") { // Если нажата "1"
        div.style.backgroundColor = "green"; // Изменить цвет на зелёный
    }
});
document.addEventListener('keyup', function (event) { // Слушатель событий при нажатии
    var div = document.getElementById("myDivSeven"); // Ищу элемент по id из HTML
    if (event.code == "Digit7") { // Если нажата "1"
        div.style.backgroundColor = "brown"; // Изменить цвет на зелёный
    }
});

document.addEventListener('keydown', function (event) { // Слушатель событий при нажатии
    var div = document.getElementById("myDivEight"); // Ищу элемент по id из HTML
    if (event.code == "Digit8") { // Если нажата "1"
        div.style.backgroundColor = "green"; // Изменить цвет на зелёный
    }
});
document.addEventListener('keyup', function (event) { // Слушатель событий при нажатии
    var div = document.getElementById("myDivEight"); // Ищу элемент по id из HTML
    if (event.code == "Digit8") { // Если нажата "1"
        div.style.backgroundColor = "brown"; // Изменить цвет на зелёный
    }
});

document.addEventListener('keydown', function (event) { // Слушатель событий при нажатии
    var div = document.getElementById("myDivNine"); // Ищу элемент по id из HTML
    if (event.code == "Digit9") { // Если нажата "1"
        div.style.backgroundColor = "green"; // Изменить цвет на зелёный
    }
});
document.addEventListener('keyup', function (event) { // Слушатель событий при нажатии
    var div = document.getElementById("myDivNine"); // Ищу элемент по id из HTML
    if (event.code == "Digit9") { // Если нажата "1"
        div.style.backgroundColor = "brown"; // Изменить цвет на зелёный
    }
});