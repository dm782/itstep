let a = [3, 7, 4]; // Создаю массив с числами
let b = []; // Создаю пустой массив
a.forEach(function (c) { // пользуюсь методом foreach и выдуманным не к чему не превязанным "c" 
    b.push(c ** 2); // push добавляет элементы в массив каждый из которых я умножаю на 2
});
console.log(b); // вывожу b с push    